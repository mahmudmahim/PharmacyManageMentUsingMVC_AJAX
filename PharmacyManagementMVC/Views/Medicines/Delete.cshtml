@model PharmacyManagementMVC.Models.Medicine

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h4 class="display-4 text-danger text-center fw-bold">Are you sure,you want to Delete?</h4>
<div>

    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.MedicineName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.MedicineName)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="d-flex justify-content-center form-control">
            <input type="submit" value="Delete" class="btn btn-danger me-2">
            @Html.ActionLink("Back To List", "Index", "Medicines", new { @class = "btn btn-info btn-sm" })
        </div>
    }
</div>
